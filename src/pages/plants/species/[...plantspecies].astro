---
import Layout from "@/layouts/Layout.astro";
import { SOUTH_INDIA_TREES } from "@/lib/constants";
export function getStaticPaths() {
  return SOUTH_INDIA_TREES.map((tree) => {
    return ({params: {plantspecies: tree.id}})
  })
}

const { plantspecies } = Astro.params;
---

<Layout title={plantspecies}>

</Layout>